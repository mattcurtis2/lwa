node_modules
.env
firebase-service-account.json
.vercel
# api/index.js is committed so Vercel can detect the serverless function
# before build (vercel.json functions pattern). Rebuild overwrites it.
